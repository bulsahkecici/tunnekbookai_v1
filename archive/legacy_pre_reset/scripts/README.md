# Archived scripts

These scripts target the pre-reset corpus, embedding, Qdrant, retrieval or book-generation
artifacts. They are preserved as historical implementation evidence and are not supported
operator commands for the current architecture.

The active commands remain in the repository-level `scripts/` directory.
