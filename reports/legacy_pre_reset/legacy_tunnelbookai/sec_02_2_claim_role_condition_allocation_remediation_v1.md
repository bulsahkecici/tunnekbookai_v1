# SEC-02-2 ARCHITECTURE V2.2 CLAIM-ROLE CONDITION ALLOCATION REMEDIATION V1

**CLOSED / GO.** Frozen preflight/postflight rehashed 21 artifacts with drift 0. Acceptance was
frozen before implementation and fixtures at SHA-256
`22f484dbcc09ef834e2fdd64d7acfa554a279de91765c07732e3b81c71d40032`.

The additive Claim-Role Condition Closure Contract runs after lexical role feasibility and before
integration or visible prose. Every semantic claim-role instance declares a registry-backed role
condition/dependency set. Each requirement must have exactly one explicit owner under mechanism
A, B, or C; the owner must be same-claim, realizable, renderer-input-capable, and valid under the
frozen validator's UNIT/PARAGRAPH scope. Ambient neighboring text has no force.

The allocated universe closes 20/20 roles, 25/25 condition requirements, and 2/2 dependencies.
The historical P0-008 allocation now rejects before prose with `CRCC_CONDITION_OWNER_MISSING`.
The generally derived safe allocation makes CLAIM_STATEMENT own `Belirli` and the tunnel-size
dependency; CASE_SCOPE owns its required unit-scoped `Belirli` and explicitly allocates the
paragraph-scoped dependency to CLAIM_STATEMENT. It preserves `ezilmiş kaya`, `sıkışan kaya`, and
`**Swelling Rock**` with `ORIGINAL_TERM_UNRESOLVED` unchanged.

Fixtures: 6/6 positive accepted; 10/10 negative rejected; false accepts 0, false rejects 0,
expected-code mismatches 0. Targeted regression: 146 tests, 0 failures. Counts: generation 0,
rendering 0, retry 0, retrieval 0, Qdrant writes 0, corpus-wide reads 0.
